#include "affichage.h"
#include "fichiers.h"

void affichage(oxy myOxy){
	//int fileState = 0;
	//FILE* pf=initFichier(filename);
	//actual = lireFichier(pf,&fileState);
	//while(fileState != EOF) {

	//}
	//finFichier(pf);
		
}

